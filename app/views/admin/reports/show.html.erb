<% content_for :page_title, "Détails du Rapport ##{@report[:id]}" %>

<!-- Page Header -->
<div class="glass-card">
  <div class="flex items-center justify-between mb-6">
    <div>
      <h1 class="gradient-heading">Détails du Rapport #<%= @report[:id] %></h1>
      <p class="text-large mt-2"><%= @report[:title] %></p>
    </div>
    <div class="button-group">
      <%= link_to admin_reports_path, class: "btn-secondary" do %>
        <svg class="btn-icon" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 19l-7-7m0 0l7-7m-7 7h18"/>
        </svg>
        Retour à la liste
      <% end %>
      <% if @report[:status] == 'completed' %>
        <%= link_to "#", class: "btn-primary" do %>
          <svg class="btn-icon" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 16v1a3 3 0 003 3h10a3 3 0 003-3v-1m-4-4l-4 4m0 0l-4-4m4 4V4"/>
          </svg>
          Télécharger
        <% end %>
      <% end %>
    </div>
  </div>
</div>

<div class="divider"></div>

<!-- Status Card -->
<div class="card">
  <div class="flex items-center justify-between mb-6">
    <h2 style="font-size: 1.875rem;">Statut du Rapport</h2>
    <% if @report[:status] == 'completed' %>
      <span class="badge-success" style="font-size: 1.125rem; padding: 0.75rem 1.5rem;">✓ Complété</span>
    <% elsif @report[:status] == 'generating' %>
      <span class="badge-info" style="font-size: 1.125rem; padding: 0.75rem 1.5rem;">⏳ En Cours</span>
    <% elsif @report[:status] == 'pending' %>
      <span class="badge-warning" style="font-size: 1.125rem; padding: 0.75rem 1.5rem;">⏸️ En Attente</span>
    <% elsif @report[:status] == 'failed' %>
      <span class="badge-danger" style="font-size: 1.125rem; padding: 0.75rem 1.5rem;">✗ Échoué</span>
    <% end %>
  </div>

  <div class="stats-grid">
    <div class="stat-card">
      <div class="stat-icon stat-icon-primary">
        <svg fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"/>
        </svg>
      </div>
      <div class="stat-content">
        <p class="stat-label">Type de Rapport</p>
        <p class="stat-value" style="font-size: 1.5rem;">
          <% case @report[:report_type] %>
          <% when 'monthly' %>
            📅 Mensuel
          <% when 'hr' %>
            👥 RH
          <% when 'time_tracking' %>
            ⏱️ Temps
          <% when 'scheduling' %>
            📆 Planning
          <% when 'anomalies' %>
            ⚠️ Anomalies
          <% when 'payroll_export' %>
            💰 Paie
          <% when 'site_performance' %>
            📊 Sites
          <% end %>
        </p>
      </div>
    </div>

    <div class="stat-card">
      <div class="stat-icon stat-icon-info">
        <svg fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 7V3m8 4V3m-9 8h10M5 21h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2z"/>
        </svg>
      </div>
      <div class="stat-content">
        <p class="stat-label">Période</p>
        <p class="stat-value" style="font-size: 1.25rem;"><%= @report[:period_start].strftime('%d/%m/%Y') %></p>
        <p class="text-sm opacity-70 mt-1">au <%= @report[:period_end].strftime('%d/%m/%Y') %></p>
      </div>
    </div>

    <div class="stat-card">
      <div class="stat-icon stat-icon-success">
        <svg fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z"/>
        </svg>
      </div>
      <div class="stat-content">
        <p class="stat-label">Généré par</p>
        <p class="stat-value" style="font-size: 1.25rem;"><%= @report[:generated_by_name] %></p>
        <p class="text-sm opacity-70 mt-1"><%= @report[:generated_at].strftime('%d/%m/%Y %H:%M') %></p>
      </div>
    </div>

    <div class="stat-card">
      <div class="stat-icon stat-icon-warning">
        <svg fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M7 21h10a2 2 0 002-2V9.414a1 1 0 00-.293-.707l-5.414-5.414A1 1 0 0012.586 3H7a2 2 0 00-2 2v14a2 2 0 002 2z"/>
        </svg>
      </div>
      <div class="stat-content">
        <p class="stat-label">Format</p>
        <p class="stat-value" style="font-size: 1.25rem;"><%= @report[:file_format] %></p>
        <% if @report[:file_size] %>
          <p class="text-sm opacity-70 mt-1"><%= @report[:file_size] %></p>
        <% end %>
      </div>
    </div>
  </div>
</div>

<div class="divider"></div>

<!-- Report Details -->
<div class="card">
  <h2 class="mb-6" style="font-size: 1.875rem;">
    <svg class="inline-block w-6 h-6 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"/>
    </svg>
    Informations Détaillées
  </h2>

  <div class="form-grid">
    <div class="form-group">
      <label>ID du Rapport</label>
      <div class="">
        <p class="font-semibold text-lg">#<%= @report[:id] %></p>
      </div>
    </div>

    <div class="form-group">
      <label>Titre</label>
      <div class="">
        <p class="font-semibold"><%= @report[:title] %></p>
      </div>
    </div>

    <div class="form-group">
      <label>Type de Rapport</label>
      <div class="">
        <% case @report[:report_type] %>
        <% when 'monthly' %>
          <span class="badge-info">📅 Rapport Mensuel</span>
        <% when 'hr' %>
          <span class="badge" style="background: #8b5cf6; color: white;">👥 Rapport RH</span>
        <% when 'time_tracking' %>
          <span class="badge-success">⏱️ Temps Travaillé</span>
        <% when 'scheduling' %>
          <span class="badge-info">📆 Planification</span>
        <% when 'anomalies' %>
          <span class="badge-warning">⚠️ Anomalies</span>
        <% when 'payroll_export' %>
          <span class="badge" style="background: #10b981; color: white;">💰 Export Paie</span>
        <% when 'site_performance' %>
          <span class="badge" style="background: #f59e0b; color: white;">📊 Performance Sites</span>
        <% end %>
      </div>
    </div>

    <div class="form-group">
      <label>Statut</label>
      <div class="">
        <% if @report[:status] == 'completed' %>
          <span class="badge-success">✓ Complété</span>
        <% elsif @report[:status] == 'generating' %>
          <span class="badge-info">⏳ En Cours de Génération</span>
        <% elsif @report[:status] == 'pending' %>
          <span class="badge-warning">⏸️ En Attente</span>
        <% elsif @report[:status] == 'failed' %>
          <span class="badge-danger">✗ Échoué</span>
        <% end %>
      </div>
    </div>

    <div class="form-group">
      <label>Date de Début</label>
      <div class="">
        <p class="font-semibold"><%= @report[:period_start].strftime('%d/%m/%Y') %></p>
        <p class="text-sm opacity-70"><%= l(@report[:period_start], format: '%A') %></p>
      </div>
    </div>

    <div class="form-group">
      <label>Date de Fin</label>
      <div class="">
        <p class="font-semibold"><%= @report[:period_end].strftime('%d/%m/%Y') %></p>
        <p class="text-sm opacity-70"><%= l(@report[:period_end], format: '%A') %></p>
      </div>
    </div>

    <div class="form-group">
      <label>Généré par</label>
      <div class="">
        <p class="font-semibold"><%= @report[:generated_by_name] %></p>
        <p class="text-sm opacity-70">Le <%= @report[:generated_at].strftime('%d/%m/%Y à %H:%M') %></p>
      </div>
    </div>

    <div class="form-group">
      <label>Format de Fichier</label>
      <div class="">
        <span class="badge"><%= @report[:file_format] %></span>
        <% if @report[:file_size] %>
          <p class="text-sm opacity-70 mt-2">Taille: <%= @report[:file_size] %></p>
        <% end %>
      </div>
    </div>
  </div>

  <% if @report[:description].present? %>
    <div class="divider"></div>
    <div class="form-group">
      <label>Description</label>
      <div class="p-4 rounded-lg border border-blue-200">
        <p class="text-base"><%= @report[:description] %></p>
      </div>
    </div>
  <% end %>
</div>

<div class="divider"></div>

<!-- Report Metrics -->
<div class="card">
  <h2 class="mb-6" style="font-size: 1.875rem;">
    <svg class="inline-block w-6 h-6 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 19v-6a2 2 0 00-2-2H5a2 2 0 00-2 2v6a2 2 0 002 2h2a2 2 0 002-2zm0 0V9a2 2 0 012-2h2a2 2 0 012 2v10m-6 0a2 2 0 002 2h2a2 2 0 002-2m0 0V5a2 2 0 012-2h2a2 2 0 012 2v14a2 2 0 01-2 2h-2a2 2 0 01-2-2z"/>
    </svg>
    Métriques du Rapport
  </h2>

  <div class="stats-grid">
    <% if @report[:total_hours] %>
      <div class="stat-card">
        <div class="stat-icon stat-icon-success">
          <svg fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z"/>
          </svg>
        </div>
        <div class="stat-content">
          <p class="stat-label">Total Heures</p>
          <p class="stat-value"><%= @report[:total_hours] %></p>
        </div>
      </div>
    <% end %>

    <% if @report[:total_agents] %>
      <div class="stat-card">
        <div class="stat-icon stat-icon-primary">
          <svg fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 20h5v-2a3 3 0 00-5.356-1.857M17 20H7m10 0v-2c0-.656-.126-1.283-.356-1.857M7 20H2v-2a3 3 0 015.356-1.857M7 20v-2c0-.656.126-1.283.356-1.857m0 0a5.002 5.002 0 019.288 0M15 7a3 3 0 11-6 0 3 3 0 016 0zm6 3a2 2 0 11-4 0 2 2 0 014 0zM7 10a2 2 0 11-4 0 2 2 0 014 0z"/>
          </svg>
        </div>
        <div class="stat-content">
          <p class="stat-label">Total Agents</p>
          <p class="stat-value"><%= @report[:total_agents] %></p>
        </div>
      </div>
    <% end %>

    <% if @report[:total_sites] %>
      <div class="stat-card">
        <div class="stat-icon stat-icon-info">
          <svg fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17.657 16.657L13.414 20.9a1.998 1.998 0 01-2.827 0l-4.244-4.243a8 8 0 1111.314 0z"/>
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 11a3 3 0 11-6 0 3 3 0 016 0z"/>
          </svg>
        </div>
        <div class="stat-content">
          <p class="stat-label">Total Sites</p>
          <p class="stat-value"><%= @report[:total_sites] %></p>
        </div>
      </div>
    <% end %>

    <% if @report[:total_absences] %>
      <div class="stat-card">
        <div class="stat-icon stat-icon-warning">
          <svg fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-3L13.732 4c-.77-1.333-2.694-1.333-3.464 0L3.34 16c-.77 1.333.192 3 1.732 3z"/>
          </svg>
        </div>
        <div class="stat-content">
          <p class="stat-label">Total Absences</p>
          <p class="stat-value"><%= @report[:total_absences] %></p>
        </div>
      </div>
    <% end %>

    <% if @report[:absence_rate] %>
      <div class="stat-card">
        <div class="stat-icon stat-icon-warning">
          <svg fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 19v-6a2 2 0 00-2-2H5a2 2 0 00-2 2v6a2 2 0 002 2h2a2 2 0 002-2zm0 0V9a2 2 0 012-2h2a2 2 0 012 2v10m-6 0a2 2 0 002 2h2a2 2 0 002-2m0 0V5a2 2 0 012-2h2a2 2 0 012 2v14a2 2 0 01-2 2h-2a2 2 0 01-2-2z"/>
          </svg>
        </div>
        <div class="stat-content">
          <p class="stat-label">Taux d'Absence</p>
          <p class="stat-value"><%= @report[:absence_rate] %>%</p>
        </div>
      </div>
    <% end %>

    <% if @report[:coverage_rate] %>
      <div class="stat-card">
        <div class="stat-icon stat-icon-success">
          <svg fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"/>
          </svg>
        </div>
        <div class="stat-content">
          <p class="stat-label">Taux de Couverture</p>
          <p class="stat-value"><%= @report[:coverage_rate] %>%</p>
        </div>
      </div>
    <% end %>

    <% if @report[:total_anomalies] %>
      <div class="stat-card">
        <div class="stat-icon stat-icon-danger">
          <svg fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-3L13.732 4c-.77-1.333-2.694-1.333-3.464 0L3.34 16c-.77 1.333.192 3 1.732 3z"/>
          </svg>
        </div>
        <div class="stat-content">
          <p class="stat-label">Total Anomalies</p>
          <p class="stat-value"><%= @report[:total_anomalies] %></p>
        </div>
      </div>
    <% end %>

    <% if @report[:resolved_anomalies] %>
      <div class="stat-card">
        <div class="stat-icon stat-icon-success">
          <svg fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"/>
          </svg>
        </div>
        <div class="stat-content">
          <p class="stat-label">Anomalies Résolues</p>
          <p class="stat-value"><%= @report[:resolved_anomalies] %></p>
        </div>
      </div>
    <% end %>

    <% if @report[:total_schedules] %>
      <div class="stat-card">
        <div class="stat-icon stat-icon-info">
          <svg fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 7V3m8 4V3m-9 8h10M5 21h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2z"/>
          </svg>
        </div>
        <div class="stat-content">
          <p class="stat-label">Total Horaires</p>
          <p class="stat-value"><%= @report[:total_schedules] %></p>
        </div>
      </div>
    <% end %>
  </div>
</div>

<% if @report[:site_name] %>
  <div class="divider"></div>

  <!-- Site Specific Information -->
  <div class="card" style="border-left: 4px solid #3b82f6;">
    <h2 class="mb-6" style="font-size: 1.875rem;">
      <svg class="inline-block w-6 h-6 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17.657 16.657L13.414 20.9a1.998 1.998 0 01-2.827 0l-4.244-4.243a8 8 0 1111.314 0z"/>
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 11a3 3 0 11-6 0 3 3 0 016 0z"/>
      </svg>
      Rapport Spécifique à un Site
    </h2>

    <div class="alert alert-info mb-6">
      <strong>Information:</strong> Ce rapport concerne uniquement le site suivant
    </div>

    <div class="form-grid">
      <div class="form-group">
        <label>Site</label>
        <div class="">
          <p class="font-semibold text-lg"><%= @report[:site_name] %></p>
          <span class="badge mt-2"><%= @report[:site_code] %></span>
        </div>
      </div>
    </div>
  </div>
<% end %>

<% if @report[:filters_applied] %>
  <div class="divider"></div>

  <!-- Filters Applied -->
  <div class="card">
    <h2 class="mb-6" style="font-size: 1.875rem;">
      <svg class="inline-block w-6 h-6 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 4a1 1 0 011-1h16a1 1 0 011 1v2.586a1 1 0 01-.293.707l-6.414 6.414a1 1 0 00-.293.707V17l-4 4v-6.586a1 1 0 00-.293-.707L3.293 7.293A1 1 0 013 6.586V4z"/>
      </svg>
      Filtres Appliqués
    </h2>

    <div class="p-4 rounded-lg border border-gray-200 bg-gray-50">
      <pre class="text-sm"><%= JSON.pretty_generate(@report[:filters_applied]) %></pre>
    </div>
  </div>
<% end %>

<div class="divider"></div>

<!-- Actions -->
<div class="glass-card">
  <h2 class="mb-4" style="font-size: 1.875rem;">Actions</h2>
  
  <div class="button-group">
    <% if @report[:status] == 'completed' %>
      <%= link_to "#", class: "btn-primary" do %>
        <svg class="btn-icon" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 16v1a3 3 0 003 3h10a3 3 0 003-3v-1m-4-4l-4 4m0 0l-4-4m4 4V4"/>
        </svg>
        Télécharger le rapport
      <% end %>

      <%= link_to "#", class: "btn-secondary" do %>
        <svg class="btn-icon" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8.684 13.342C8.886 12.938 9 12.482 9 12c0-.482-.114-.938-.316-1.342m0 2.684a3 3 0 110-2.684m0 2.684l6.632 3.316m-6.632-6l6.632-3.316m0 0a3 3 0 105.367-2.684 3 3 0 00-5.367 2.684zm0 9.316a3 3 0 105.368 2.684 3 3 0 00-5.368-2.684z"/>
        </svg>
        Partager
      <% end %>
    <% end %>

    <%= link_to admin_reports_path, class: "btn-secondary" do %>
      <svg class="btn-icon" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 19l-7-7m0 0l7-7m-7 7h18"/>
      </svg>
      Retour à la liste
    <% end %>

    <%= button_to "#", method: :delete, data: { confirm: 'Êtes-vous sûr de vouloir supprimer ce rapport ?' }, class: "btn-danger" do %>
      <svg class="btn-icon" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 7l-.867 12.142A2 2 0 0116.138 21H7.862a2 2 0 01-1.995-1.858L5 7m5 4v6m4-6v6m1-10V4a1 1 0 00-1-1h-4a1 1 0 00-1 1v3M4 7h16"/>
      </svg>
      Supprimer ce rapport
    <% end %>
  </div>
</div>
