<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Rapport Mensuel - <%= start_date.strftime('%B %Y') %></title>
  <style>
    body {
      font-family: Arial, sans-serif;
      margin: 20px;
      color: #333;
    }
    .header {
      text-align: center;
      margin-bottom: 30px;
      border-bottom: 2px solid #2563eb;
      padding-bottom: 20px;
    }
    .header h1 {
      color: #1e40af;
      margin-bottom: 10px;
    }
    .header p {
      margin: 5px 0;
      color: #666;
    }
    .stats-section {
      background-color: #f3f4f6;
      padding: 20px;
      margin: 20px 0;
      border-radius: 8px;
    }
    .stats-section h2 {
      color: #1e40af;
      margin-bottom: 15px;
      font-size: 18px;
    }
    .stats-grid {
      display: grid;
      grid-template-columns: repeat(2, 1fr);
      gap: 15px;
    }
    .stat-item {
      background-color: white;
      padding: 15px;
      border-radius: 6px;
      border-left: 4px solid #2563eb;
    }
    .stat-label {
      font-size: 12px;
      color: #666;
      text-transform: uppercase;
      margin-bottom: 5px;
    }
    .stat-value {
      font-size: 24px;
      font-weight: bold;
      color: #1e40af;
    }
    .section {
      margin: 30px 0;
    }
    .section h2 {
      color: #1e40af;
      margin-bottom: 15px;
      font-size: 18px;
      border-bottom: 1px solid #e5e7eb;
      padding-bottom: 10px;
    }
    table {
      width: 100%;
      border-collapse: collapse;
      margin: 15px 0;
      background-color: white;
    }
    th {
      background-color: #1e40af;
      color: white;
      padding: 12px;
      text-align: left;
      font-size: 12px;
      text-transform: uppercase;
    }
    td {
      padding: 10px 12px;
      border-bottom: 1px solid #e5e7eb;
      font-size: 14px;
    }
    tr:hover {
      background-color: #f9fafb;
    }
    .status-badge {
      display: inline-block;
      padding: 4px 8px;
      border-radius: 4px;
      font-size: 11px;
      font-weight: bold;
    }
    .status-completed {
      background-color: #d1fae5;
      color: #065f46;
    }
    .status-active {
      background-color: #fef3c7;
      color: #92400e;
    }
    .anomaly-high {
      background-color: #fee2e2;
      color: #991b1b;
    }
    .anomaly-medium {
      background-color: #fed7aa;
      color: #9a3412;
    }
    .anomaly-low {
      background-color: #fef3c7;
      color: #92400e;
    }
    .footer {
      margin-top: 40px;
      padding-top: 20px;
      border-top: 1px solid #e5e7eb;
      text-align: center;
      font-size: 12px;
      color: #666;
    }
    @media print {
      body {
        margin: 0;
      }
      .page-break {
        page-break-after: always;
      }
    }
  </style>
</head>
<body>
  <!-- Header -->
  <div class="header">
    <h1>Rapport Mensuel de Pointage</h1>
    <p><strong>Période:</strong> <%= start_date.strftime('%d/%m/%Y') %> - <%= end_date.strftime('%d/%m/%Y') %></p>
    <p><strong>Généré le:</strong> <%= generated_at.strftime('%d/%m/%Y à %H:%M') %></p>
    <p><strong>Généré par:</strong> <%= generated_by %></p>
  </div>

  <!-- Statistics Section -->
  <div class="stats-section">
    <h2>Statistiques Générales</h2>
    <div class="stats-grid">
      <div class="stat-item">
        <div class="stat-label">Total Heures Travaillées</div>
        <div class="stat-value"><%= total_hours.round(2) %> h</div>
      </div>
      <div class="stat-item">
        <div class="stat-label">Nombre d'Agents</div>
        <div class="stat-value"><%= total_agents %></div>
      </div>
      <div class="stat-item">
        <div class="stat-label">Nombre de Sites</div>
        <div class="stat-value"><%= total_sites %></div>
      </div>
      <div class="stat-item">
        <div class="stat-label">Nombre de Pointages</div>
        <div class="stat-value"><%= time_entries.count %></div>
      </div>
    </div>
  </div>

  <!-- Time Entries Details -->
  <div class="section">
    <h2>Détail des Pointages</h2>
    <table>
      <thead>
        <tr>
          <th>ID</th>
          <th>Agent</th>
          <th>N° Employé</th>
          <th>Site</th>
          <th>Date</th>
          <th>Arrivée</th>
          <th>Départ</th>
          <th>Durée (h)</th>
          <th>Statut</th>
        </tr>
      </thead>
      <tbody>
        <% time_entries.each do |entry| %>
          <tr>
            <td><%= entry.id %></td>
            <td><%= entry.user.full_name %></td>
            <td><%= entry.user.employee_number %></td>
            <td><%= entry.site.name %></td>
            <td><%= entry.clocked_in_at.strftime('%d/%m/%Y') %></td>
            <td><%= entry.clocked_in_at.strftime('%H:%M') %></td>
            <td><%= entry.clocked_out_at ? entry.clocked_out_at.strftime('%H:%M') : 'En cours' %></td>
            <td><%= entry.duration_minutes ? (entry.duration_minutes / 60.0).round(2) : '-' %></td>
            <td>
              <span class="status-badge status-<%= entry.status %>">
                <%= entry.status %>
              </span>
            </td>
          </tr>
        <% end %>
      </tbody>
    </table>
  </div>

  <div class="page-break"></div>

  <!-- Hours by Agent -->
  <div class="section">
    <h2>Heures par Agent</h2>
    <table>
      <thead>
        <tr>
          <th>Agent</th>
          <th>N° Employé</th>
          <th>Total Heures</th>
          <th>Nombre de Pointages</th>
        </tr>
      </thead>
      <tbody>
        <% time_entries.group_by(&:user).each do |user, entries| %>
          <% total_minutes = entries.map(&:duration_minutes).compact.sum %>
          <tr>
            <td><%= user.full_name %></td>
            <td><%= user.employee_number %></td>
            <td><%= (total_minutes / 60.0).round(2) %> h</td>
            <td><%= entries.count %></td>
          </tr>
        <% end %>
      </tbody>
    </table>
  </div>

  <!-- Hours by Site -->
  <div class="section">
    <h2>Heures par Site</h2>
    <table>
      <thead>
        <tr>
          <th>Site</th>
          <th>Code</th>
          <th>Total Heures</th>
          <th>Nombre de Pointages</th>
        </tr>
      </thead>
      <tbody>
        <% time_entries.group_by(&:site).each do |site, entries| %>
          <% total_minutes = entries.map(&:duration_minutes).compact.sum %>
          <tr>
            <td><%= site.name %></td>
            <td><%= site.code %></td>
            <td><%= (total_minutes / 60.0).round(2) %> h</td>
            <td><%= entries.count %></td>
          </tr>
        <% end %>
      </tbody>
    </table>
  </div>

  <!-- Anomalies Section (if included) -->
  <% if anomalies.any? %>
    <div class="page-break"></div>
    <div class="section">
      <h2>Anomalies Détectées</h2>
      <table>
        <thead>
          <tr>
            <th>Type</th>
            <th>Agent</th>
            <th>Description</th>
            <th>Sévérité</th>
            <th>Date</th>
            <th>Résolu</th>
          </tr>
        </thead>
        <tbody>
          <% anomalies.each do |anomaly| %>
            <tr>
              <td><%= anomaly.anomaly_type %></td>
              <td><%= anomaly.user ? anomaly.user.full_name : '-' %></td>
              <td><%= anomaly.description %></td>
              <td>
                <span class="status-badge anomaly-<%= anomaly.severity %>">
                  <%= anomaly.severity %>
                </span>
              </td>
              <td><%= anomaly.created_at.strftime('%d/%m/%Y %H:%M') %></td>
              <td><%= anomaly.resolved? ? '✓ Oui' : '✗ Non' %></td>
            </tr>
          <% end %>
        </tbody>
      </table>
    </div>
  <% end %>

  <!-- Footer -->
  <div class="footer">
    <p>Ce rapport a été généré automatiquement par le système MadyProClean</p>
    <p>Pour toute question, veuillez contacter votre administrateur système</p>
  </div>
</body>
</html>
