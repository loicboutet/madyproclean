<% content_for :page_title, "Plannings de l'Équipe" %>

<!-- Page Header -->
<div class="glass-card">
  <div class="flex items-center justify-between mb-6">
    <div>
      <h1 class="gradient-heading">Plannings de l'Équipe</h1>
      <p class="text-large mt-2">Consultez les plannings et affectations des agents de votre équipe</p>
    </div>
  </div>

<%= render 'shared/schedules/statistics_cards', total_label: 'Total Plannings' %>

<div class="divider"></div>
  <%= render 'shared/schedules/filters', 
             label_type: 'plannings',
             search_placeholder: 'Agent, site...',
             reset_path: manager_schedules_path %>
</div>


<div class="divider"></div>

<!-- Schedules Table -->
<div class="card">
  <div class="flex items-center justify-between mb-6">
    <div>
      <h2 class="mb-2" style="font-size: 1.875rem;">Liste des Plannings</h2>
      <p class="text-large">Visualisez tous les plannings des agents avec leurs détails</p>
    </div>
  </div>

  <div class="table-responsive">
    <table>
      <thead>
        <tr>
          <th>ID</th>
          <th>Date Planifiée</th>
          <th>Agent</th>
          <th>Site</th>
          <th>Horaires</th>
          <th>Durée</th>
          <th>Statut</th>
          <th>Remplacement</th>
          <th>Actions</th>
        </tr>
      </thead>
      <tbody>
        <% if @schedules.empty? %>
          <tr>
            <td colspan="9" style="text-align: center; padding: 2rem;">
              <div class="alert alert-warning" style="display: inline-block;">
                <strong>Aucun planning trouvé</strong> avec les filtres actuels
              </div>
            </td>
          </tr>
        <% else %>
          <% @schedules.each do |schedule| %>
            <tr>
              <td><strong>#<%= schedule[:id] %></strong></td>
              <td>
                <div>
                  <p class="font-semibold"><%= schedule[:scheduled_date].strftime('%d/%m/%Y') %></p>
                  <p class="text-sm opacity-70"><%= l(schedule[:scheduled_date], format: '%A') %></p>
                </div>
              </td>
              <td>
                <div>
                  <p class="font-semibold"><%= schedule[:user_name] %></p>
                  <p class="text-sm opacity-70"><%= schedule[:employee_number] %></p>
                </div>
              </td>
              <td>
                <div>
                  <p class="font-semibold"><%= schedule[:site_name] %></p>
                  <span class="badge"><%= schedule[:site_code] %></span>
                </div>
              </td>
              <td>
                <div>
                  <p class="font-semibold"><%= schedule[:start_time] %> - <%= schedule[:end_time] %></p>
                  <p class="text-sm opacity-70">Horaire de travail</p>
                </div>
              </td>
              <td>
                <% 
                  start_parts = schedule[:start_time].split(':')
                  end_parts = schedule[:end_time].split(':')
                  duration = (end_parts[0].to_i - start_parts[0].to_i) + ((end_parts[1].to_i - start_parts[1].to_i) / 60.0)
                %>
                <div>
                  <p class="font-semibold"><%= '%.1f' % duration %>h</p>
                  <p class="text-sm opacity-70"><%= (duration * 60).to_i %> min</p>
                </div>
              </td>
              <td>
                <%= render 'shared/schedules/status_badge', status: schedule[:status], namespace: 'manager' %>
              </td>
              <td>
                <% if schedule[:replaced_by_id] %>
                  <div>
                    <p class="font-semibold text-orange-600">🔄 <%= schedule[:replaced_by_name] %></p>
                    <p class="text-sm opacity-70">Remplaçant</p>
                  </div>
                <% else %>
                  <span class="text-sm opacity-50">—</span>
                <% end %>
              </td>
              <td>
                <div class="dropdown-wrapper" data-controller="dropdown">
                  <button class="dropdown-trigger" data-action="click->dropdown#toggle" title="Actions">
                    <svg fill="none" stroke="currentColor" viewBox="0 0 24 24">
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 5v.01M12 12v.01M12 19v.01M12 6a1 1 0 110-2 1 1 0 010 2zm0 7a1 1 0 110-2 1 1 0 010 2zm0 7a1 1 0 110-2 1 1 0 010 2z"/>
                    </svg>
                  </button>
                  
                  <div class="dropdown-menu hidden" data-dropdown-target="menu">
                    <%= link_to manager_schedule_path(schedule[:id]), class: "dropdown-item" do %>
                      <svg fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 12a3 3 0 11-6 0 3 3 0 016 0z"/>
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M2.458 12C3.732 7.943 7.523 5 12 5c4.478 0 8.268 2.943 9.542 7-1.274 4.057-5.064 7-9.542 7-4.477 0-8.268-2.943-9.542-7z"/>
                      </svg>
                      <span>Voir les détails</span>
                    <% end %>
                  </div>
                </div>
              </td>
            </tr>
          <% end %>
        <% end %>
      </tbody>
    </table>
  </div>

  <!-- Pagination -->
  <%= render 'shared/pagination' %>
</div>

<div class="divider"></div>

<!-- Information Card -->
<div class="glass-card">
  <h2 class="mb-4" style="font-size: 1.875rem;">Informations</h2>
  <div class="alert alert-info mb-6">
    <strong>À propos:</strong> Cette page vous permet de consulter les plannings de votre équipe. Les filtres sont affichés à titre indicatif.
  </div>
  
  <div class="alert alert-warning">
    <strong>⚠ Note:</strong> Les managers peuvent consulter les plannings mais la gestion complète (création, modification, suppression) est réservée aux administrateurs.
  </div>
</div>
